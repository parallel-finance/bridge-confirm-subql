{
  "name": "bridge-confirm-subql",
  "version": "0.0.4",
  "description": "Parallel substrate bridge confirmation service",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc -b",
    "prepack": "rm -rf dist && npm build",
    "ipfs:publish": "subql publish",
    "test": "jest",
    "codegen": "./node_modules/.bin/subql codegen"
  },
  "homepage": "https://github.com/parallel-finance/bridge-confirm-subql",
  "repository": "https://github.com/parallel-finance/bridge-confirm-subql",
  "files": [
    "dist",
    "schema.graphql",
    "project.yaml"
  ],
  "author": "Parallel Team",
  "license": "MIT",
  "devDependencies": {
    "@polkadot/api": "^7",
    "@subql/types": "latest",
    "typescript": "^4.1.3",
    "@subql/cli": "latest"
  }
}
